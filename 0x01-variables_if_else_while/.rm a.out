#include <stdio.h>

/**
 * main - Prints all possible combinations of two two-digit numbers.
 *
 * Return: Code returns 0 if successfully run.
 */
int main(void)
{
	int a;
	int b;
	int c;
	int d;

	/**for (a = 48; a <= 57; a++)
	{
		for (b = 48; b = 57; b++)
		{
			if (a != b)
			{
				putchar(a);
				putchar(b);
				putchar(32);
			}
		}
	}
*/
	for (a = 48; a <= 57; a++)
	{
		for (b = 48; b = 57; b++)
		{
			if (a != b)
			{
				putchar(a);
				putchar(b);
				putchar(44);
			}
		}
	}
	putchar('\n');

	return (0);
}
