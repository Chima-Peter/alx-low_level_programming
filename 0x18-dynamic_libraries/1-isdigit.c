#include "main.h"

/**
 * _isdigit - Write a function that checks for a digit (0 through 9).
 *
 * @c: The interger.
 *
 * Return: Code returns 1 if digit, else 0.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
