#include "main.h"
/**
 * mod - Finds the remainder of the division of 2 integer.
 *
 * @a: First integer
 *
 * @b: Second integer
 *
 * Return: the result
 */
int mod(int a, int b)
{
	return (a % b);
}
