!<arch>
0-isupper.c/    0           0     0     644     85        `

#include "main.h"

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (0);
}

0-memset.c/     0           0     0     644     138       `
#include "main.h"

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;
	for (i = 0; i < n; i++)
		s[i] = b;
	return (s);
}

0-strcat.c/     0           0     0     644     208       `


#include "main.h"

char *_strcat(char *dest, char *src)
{
	int i = 0;

	int j = 0;

	while (dest[j] != '\0')
	{
		j++;
	}
	
	while (src[i] != '\0')
	{
		dest[j] = src[i];
		i++;
		j++;
	}
	return (dest);
}
100-atoi.c/     0           0     0     644     467       `

#include "main.h"

int _atoi(char *s)
	{
	int i, d, n, len, f, digit;

	i = 0;
	d = 0;
	n = 0;
	len = 0;
	f = 0;
	digit = 0;

	while (s[len] != '\0')
		len++;

	while (i < len && f == 0)
	{
		if (s[i] == '-')
			++d;

		if (s[i] >= '0' && s[i] <= '9')
		{
			digit = s[i] - '0';
			if (d % 2)
				digit = -digit;
			n = n * 10 + digit;
			f = 1;
			if (s[i + 1] < '0' || s[i + 1] > '9')
				break;
			f = 0;
		}
		i++;
	}

	if (f == 0)
		return (0);

	return (n);
}

1-isdigit.c/    0           0     0     644     76        `

#include "main.h"

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
1-memcpy.c/     0           0     0     644     159       `

#include "main.h"

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[i] = src[i];
	}
	return (0);
}

1-strncat.c/    0           0     0     644     222       `

#include "main.h"

char *_strncat(char *dest, char *src, int n)
{
	int i = 0;

	int j = 0;

	while (dest[j] != '\0')
	{
		j++;
	}
	while (src[i] != '\0' && j < n)
	{
		dest[j] = src[i];
		i++;
		j++;
	}
	return (dest);
}
2-strchr.c/     0           0     0     644     172       `

#include "main.h"

char *_strchr(char *s, char c)
{
	int i;

	while (1)
	{
		i = *s++;

		if (i == c)
		{
			return (s - 1);
		}
		if (i == 0)
		{
			return (0);
		}
	}
}
2-strlen.c/     0           0     0     644     104       `

#include "main.h"

int _strlen(char *s)
{
	int i = 0;

	while (*s != '\0')
	{
		i++;
	}
	return (0);
}
2-strncpy.c/    0           0     0     644     210       `

#include "main.h"

char *_strncpy(char *dest, char *src, int n)
{
	int i = 0;

	while (src[i] != '\0' && i < n)
	{
		dest[i] = src[i];
		i++;
	}
	while (i < n)
	{
		dest[i] = '\0';
		i++;
	}
	return (dest);
}
3-islower.c/    0           0     0     644     76        `

#include "main.h"

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
3-puts.c/       0           0     0     644     106       `

#include "main.h"

void _puts(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
		_putchar(s[i]);
	}
}
3-strcmp.c/     0           0     0     644     145       `

#include "main.h"

int _strcmp(char *s1, char *s2)
{
	while (*s1 == *s2)
	{
		if (*s1 == '\0')
			return (0);
		s1++;
		s2++;
	}
	return (0);
}

3-strspn.c/     0           0     0     644     295       `

#include "main.h"

unsigned int _strspn(char *s, char *accept)
	{
	int i, c, j, k;

	c = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
		k = 0;
		for (j = 0; accept[j] != '\0'; j++)
		{
			if (s[i] == accept[j])
			{
				c++;
				k = 1;
			}
		}
		if (k == 0)
		{
			return (c);
		}
	}
	return (0);
}

4-isalpha.c/    0           0     0     644     100       `

#include "main.h"

int _isalpha(int c)
{
	return (c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z');
}
4-strpbrk.c/    0           0     0     644     227       `

#include "main.h"

char *_strpbrk(char *s, char *accept)
{
		int i, k;

	for (i = 0; *s != '\0'; i++)
	{
		for (k = 0; accept[k] != '\0'; k++)
		{
			if (*s == accept[k])
			{
				return (s);
			}
		}
		s++;
	}
	return (0);
}

5-strstr.c/     0           0     0     644     278       `

#include "main.h"

char *_strstr(char *haystack, char *needle)
{
	int i, j;

	for (i = 0; haystack[i] != '\0'; i++)
	{
		for (j = 0; needle[j] != '\0'; j++)
		{
			if (haystack[i + j] != needle[j])
				break;
		}
			if (!needle[j])
				return (&haystack[i]);
	}
	return (0);
}
6-abs.c/        0           0     0     644     135       `

#include "main.h"

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	} else if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	return (0);
}

9-strcpy.c/     0           0     0     644     204       `

#include "main.h"

char *_strcpy(char *dest, char *src)
{
	int i;

	int j = 0;

	while (src[j] != '\0')
	{
		j++;
	}
	for (i = 0; i < j; i++)
	{
		dest[i] = src[i];
	}
	dest[i] = '\0';
	return (dest);
}
main.c/         0           0     0     644     148       `
#include "main.h"

int main(void)
{
    _puts("\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick");
    return (0);
}
_putchar.c/     0           0     0     644     91        `
#include <unistd.h>
#include "main.h"

int _putchar(char c)
{
	return (write(1, &c, 1));
}

